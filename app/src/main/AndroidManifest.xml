<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eus.ehu.egunerokoapp.tta.egunerokoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentacion.RegistroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentacion.LoginActivity" />
        <activity android:name=".presentacion.MenuActivity" />
        <activity android:name=".presentacion.GoizeanActivity" />
        <activity android:name=".presentacion.GozogozoaActivity" />
        <activity android:name=".presentacion.ZerdiozuActivity" />
        <activity android:name=".presentacion.EkintzakActivity" />
        <activity android:name=".presentacion.PuntuazioaActivity" />
        <activity android:name=".presentacion.BagoazActivity"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>